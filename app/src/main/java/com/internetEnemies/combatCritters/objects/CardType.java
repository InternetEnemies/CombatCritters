package com.internetEnemies.combatCritters.objects;

/**
 * enum for the types of cards
 *
 * ! I really don't like this tbh but its the least bad solution I can think of tbh
 */
public enum CardType {
    CRITTER,
    ITEM
}
